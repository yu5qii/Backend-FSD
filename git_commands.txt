git command
local repo to cloud repo-
git init
git status
gir add file name/.
git commit -m "msg"
git remote add original githubrepo url
git branch -M main
git push -u origin main

cloud to local repo-
git clone url

other commands-
git log //no of commits
git checkout // checks branch